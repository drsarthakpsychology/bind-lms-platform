node_modules/
.next/
.env*.local
.env
*.tsbuildinfo
next-env.d.ts
.DS_Store
.vercel
.claude/worktrees/

# Corpus raw fetched data (regenerable via npm run corpus:*)
scripts/corpus/raw/
scripts/corpus/drafted-cases.json

# FDA label raw fetch data (regenerable via npm run psych:fda-*)
scripts/psychopharm/fda/*.html
scripts/psychopharm/fda/*.json

# Knowledge-layer raw book cache (licensed PDFs + extracted text; regenerable)
scripts/knowledge/cache/
.e2e-state.json
test-results/
playwright-report/
.claude/.continue_count
STOP_CLAUDE
night-runner.log
.claude/DIRECTIVE.md
.claude/settings.json.bak
.claude/settings.json.NEW
.claude/.nwcount
# Local Supabase state
supabase/.branches/
supabase/.temp/

# Nightwork hook marker (empty, regenerated each run)
.claude/.nwverified

# STRIX pentest output (generated, may contain findings)
strix_runs/
# Playwright-captured mobile screenshots (regenerate via the visual-pass spec)
screenshots/

# local voice-sample artifacts (listen, don't commit)
docs/samples/

# Python build artifacts
__pycache__/
*.pyc
